server:
  error:
    include-stacktrace: never

spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/mysql?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true'
    usernamne: 'root'
    password: 'root'
    driver-class-name: 'com.mysql.cj.jdbc.Driver'
  jpa:
    hibernate:
      ddl-auto: update